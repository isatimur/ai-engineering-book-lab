# Session Handoff — 2026-06-04

Pick up here in a fresh session. Everything below is the live state.

## Git state
- Branch `main`, tree clean.
- **4 unpushed commits** (local ahead of origin/main):
  - `f333c07` anchor: expand Zeghidour claim-29 quote
  - `9568e8f` anchor: expand 6 truncated quotes (claims 11,12,16,19,36)
  - `0670177` anchor: upgrade 4 weak quotes (claims 3,9,26,30)
  - `869bdb6` prose(ch2,3,4,8,9): review fixes
- **Pending decision:** push `main` to deploy these (the push triggers the `website-deploy` GitHub Action → Vercel prod, build path already fixed/verified). The repo is **public**, corpus already published.

## What's DONE (this multi-turn session)
- **De-slop pass** (book-wide humanness): all 10 chapters, committed + live.
- **Usefulness hybrid** (book-wide): all 10 chapters — in-prose sharpenings + "What to do with this" sections, fidelity-bound, committed + live. Spec: `docs/superpowers/specs/2026-06-03-usefulness-pilot-design.md`.
- **SEO routing** (vite-react-ssg, 36 prerendered pages, per-page meta, sitemap, cleanUrls): merged (PR #1), live. Vercel build fix: repo-root `vercel.json` + `website/vercel.json` (`buildCommand: npm run build`).
- **Corpus ingest**: 708 → **741** talks (33 new via yt-dlp; 3 had no subtitles). Corpus now **git-tracked** (was untracked); transcripts (`99_Meta/transcripts/raw|plain`, `batches`) stay gitignored.
- **Claims re-anchoring** (this session): original 57 ≤3-word fragments + 4 worst review-findings (#3,9,26,30) + 6 truncations (#11,12,16,19,36) + Zeghidour #29. Ledger = **150/150 anchors confidence: high**.
- **Text review fixes** (ch2,3,4,8,9): ch4 dedup, ch9 "10x is a provocation" beat, ch3 Codex→product-docs, ch2 swyx intro, ch8 TTS softened.

## OPEN items (do these next)
1. **Claims #40–42 redundancy audit.** A review found claims 41 & 42 may be saying the same thing and share ~3 anchors verbatim. Read the three claim blocks in `claims/Claims Ledger.md` (grep `^## 40)` … `## 43)`); decide whether to differentiate evidence or merge 41/42. Analysis → possible ledger edit + `build_evidence.py`.
2. **#29 Debnath (`hwCmfThIiS4`) — known limitation.** Its masking quote couldn't be cleaned (VTT artifact "…to that" in the relevant region). Left as the grammatical talk-intro; claim 29 stands on Zeghidour/Harries/Kramer. Only revisit if you want to swap the source.
3. **Diagrams (needs Excalidraw re-export tooling):**
   - `diagrams/ch10-fig2-constrained-delegation.excalidraw`: text "donb" → "done", re-export PNG.
   - 5 concept cards have right-edge subtitle clipping (widen canvas + re-export): `14-one-shot-ai-failure`, `15-software-factory`, `16-spec-driven-development`, `17-vibe-coding`, `18-voice-agents`.
   - NOTE: the image subagent's "28 broken manifest paths" was a **FALSE POSITIVE** (prod serves `/diagrams/...` 200; it compared source dir vs built `public/diagrams/`). Do not act on it.
4. **MASH re-score** (BLOCKED on you): needs `ANTHROPIC_API_KEY` in shell + spend (~$4–5).
   - `cd ~/Dev/LifeOS/book-mash && poetry run book-mash measure --config ~/Dev/LifeOS/knowledge-bases/ai-engineer-book/book-mash.toml`
   - then `cd ~/Dev/LifeOS/knowledge-bases/ai-engineer-book && python3 99_Meta/scripts/build_judge_scores.py`
   - book-mash `_CONCURRENCY` lowered 8→3 (uncommitted in book-mash) to avoid the partial-run rate-limit. After it runs, I read `website/src/data/judge-scores.json` and report usefulness/humanness deltas.

## Method notes (for the re-anchoring / fixes)
- **Anchor CLI:** `python3 99_Meta/scripts/anchor/cli.py <video_id> "<verbatim phrase>"` → JSON {start,end,quote,confidence}. Use the returned quote VERBATIM. For ids starting with `-`, use `cli.py -- -<id> "phrase"`.
- **Transcript keyword context helper:** `/tmp/kw.py <video_id> "<keyword>" [chars]` (may not persist across sessions — recreate: reads `99_Meta/transcripts/plain/<id>.txt`, prints ~140 chars around matches).
- **Regenerate evidence:** `python3 99_Meta/scripts/anchor/build_evidence.py` (writes `website/src/evidence.json`).
- **drafting ↔ content sync:** edit `public/drafting/Chapter N - ….md` (canonical, MASH-judged), then propagate the body (from the 2nd `# Chapter N —` heading to EOF) to `website/src/content/chapter-NN.md` (the rendered copy). They must match.
- **Stats regen:** `python3 99_Meta/scripts/build_stats.py`. **Heads-up:** a `stats-regen` CI bot pushes `[skip ci]` stat commits to origin/main — expect to `git fetch` + reconcile (regenerate stats to resolve the trivial conflict) before pushing.
- **Fidelity rule (book-wide):** never fabricate; every quote/claim/sharpening must trace to a source already present. Quoted speech is verbatim.

## Local dev
- `cd website && DISABLE_HMR=true npm run dev` → was running on :3002 (may be dead now). Routes: `/read/NN-slug` (e.g. `/read/05-context`), `/visual-guide/concepts/:slug`.
