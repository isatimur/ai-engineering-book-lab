# Session Handoff — 2026-06-11

Pick up here in a fresh session. Everything below is the live state.

## Snapshot
- **Corpus 757** · **50 claims / 180 anchors** (all high-confidence) · **10 chapters**.
- Latest MASH run: `2026-06-10-145000-0cb7` (extracted into `judge-scores.json`).
- Branch `main`, tree clean. **Note: this repo is being edited by multiple concurrent sessions** — another is actively shipping frontend (`/assess`, `/enterprise`, `/workshop` pages) and synthesizing claims. Always `git fetch` + rebase before pushing; keep edits append-only / new-file where possible.

## What shipped this session (lots)
1. **Usefulness fork resolved (a+b+c)** — substantive-usefulness rollup in `build_judge_scores.py` (drops headings + short connective bridges; 73/354 ≈ 21%, conservative), `/quality` genre-ceiling note, 6 fidelity-bound filler cuts. Book usefulness 40.1 → substantive 46.0.
2. **Book-wide quality audit (3 parallel auditors)** + remediation: 7 hostile-editor prose fixes; **de-fabricated a Joel Hron quote** that wasn't in its source transcript (the namesake talk) → honest paraphrase; documented the #29 VTT artifact; fixed stale public counts.
3. **Build root-cause fix** — `gen-data.mjs` no longer auto-runs `build_judge_scores.py` on web builds (it was silently drifting the snapshot-pinned scores against edited drafting files). judge-scores.json is now a manually-regenerated committed artifact like evidence.json. **See memory `judge-scores-snapshot-pinned`.**
4. **Corpus 741 → 757** — ingested 16 new on-theme talks (batch `recent-742-757`) via `update_ai_engineer_channel.py`.
5. **Tic-diversification pass** — the prose audit's #1 lever; 5 signature devices cut to budget (e.g. "The trap is" 7→2).
6. **Claims synthesis 151 → 180 anchors** — mined the 16 new talks (3 parallel miners), every quote re-verified verbatim via the anchor CLI. Added anchors to claims #8/#10/#13/#14/#15/#17/#19/#20/#23/#26/#27/#31/#36, and a **new claim #50 (agent commerce)** from 3 sources (Behrens #200, Ionic #503, Stripe #745).
7. **ch5 roster-thinning** — last deferred prose-audit item; removed "same move/logic/argument/observation" repetition + premature double-citations.
8. **MASH run 4 (`0cb7`, $0.61 warm)** — validated the lift: usefulness 40.1 → 42.1 (trend 34.1 → 40.1 → 42.1); biggest gains ch04/05/06/10.
9. **P1 heavy-judge throttle** — book-mash `3926f25`: `_HEAVY_CONCURRENCY=1` semaphore serializes humanness + claim_defensibility so they stop blowing the per-key TPM ceiling (regression test added; suite 67 green).
10. **Diagram source counts 741 → 757** — JSON-aware fix to 5 `.excalidraw` sources (text fields only).
11. **Cross-chapter search** — new `/search` page (`src/lib/search.ts` + 11 tests, `pages/Search.tsx`, TopNav link); client-side, no deps, `noindex`.

## OPEN — gated on you
1. **MASH coverage re-run (verify the P1 fix).** book-mash `3926f25` should close the humanness (70%) / claim_defensibility (71%) gaps, but needs one run to confirm ~100%. Run it yourself (~$0.60 warm), then I extract:
   ```bash
   cd ~/Dev/LifeOS/book-mash && ANTHROPIC_API_KEY='sk-…' \
     poetry run book-mash measure --config ~/Dev/LifeOS/knowledge-bases/ai-engineer-book/book-mash.toml
   ```
   Then: `python3 99_Meta/scripts/build_judge_scores.py --version-id $(git rev-parse --short HEAD)`.
2. **Diagram PNG re-export.** Sources now say 757 but the exported PNGs still bake 741. A faithful programmatic export is a real sub-project (needs bundled excalidraw + React + Excalifont; esm.sh doesn't expose the export API headless, and a from-scratch SVG render would drift). Manual re-export on excalidraw.com of the 5 corrected sources is the pragmatic path — or finish the bundled exporter.
3. **Leftover `git stash` entries** (2 autostashes + 1 named `main-uncommitted-before-merge` pre-rework backup) cause the recurring stats-file conflicts on rebase. Inspect with `git stash show -p stash@{N}`; drop when you're sure they're disposable (I left them — couldn't prove safe).

## Method notes (unchanged + new)
- **Anchor CLI:** `python3 99_Meta/scripts/anchor/cli.py <video_id> "<verbatim phrase>"` → {start,end,quote,confidence}. It is the fabrication gate — only commit quotes it verifies.
- **Evidence regen:** `python3 99_Meta/scripts/anchor/build_evidence.py`. **Stats:** `python3 99_Meta/scripts/build_stats.py`.
- **judge-scores is snapshot-pinned** — never regenerate it against edited drafting files (it drifts). Regenerate only when the working tree matches the scored snapshot; for an old run after edits, `git checkout <sha> -- public/drafting/` first.
- **Build churn to discard before committing:** `npm run build` regenerates `website/public/diagrams/*` (compressed PNGs from the perf work), `src/data/versions*`, and `sitemap.xml`. `git checkout HEAD -- …` these unless intentionally updating them.
- **Corpus update:** `update_ai_engineer_channel.py --dry-run` to preview the channel diff; drop `--dry-run` to ingest (transcripts are gitignored).
