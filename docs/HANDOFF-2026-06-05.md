# Session Handoff — 2026-06-05

Pick up here in a fresh session. Everything below is the live state.

## Git state
- Branch `main`, tree clean (only `.claude/` untracked, gitignored).
- **9 unpushed commits** (local ahead of origin/main):
  - `0307dab` chore(diagrams): update corpus count 693→741 across all diagram files and templates
  - `6e445df` feat(diagrams): add four-act divider diagrams with generator script
  - `b602392` chore(versions): add ch02/03/04/08/09 prose-review version snapshots; bump judge-scores pointer
  - `e3b6fc2` feat(diagrams): add ch05 fig4 (misassembly vs hallucination) and fig5 (RAG is 4 jobs)
  - `c70ca22` feat(seo): add llms.txt, llms-full.txt, and per-chapter markdown for LLM discoverability
  - `33febe7` feat(diagrams): add gap inline figures ch01/06/07/08/09 fig4 (+ ch08-fig5)
  - `cd9c8c9` chore(build): sync diagram-manifest.json + update sitemap timestamps
  - `5d957e9` feat(visual-guide): add four-act dividers section; wire dividers into manifest + VisualGuide
  - `68c6467` chore(scripts): add dividers to sync-diagrams.sh; update 2 re-exported PNGs
- **Pending decision:** push `main` to deploy (push triggers `website-deploy` GitHub Action → Vercel prod). Repo is public.

## What's DONE this session (2026-06-05)
- **Diagram corpus count** 693→741 across all `.excalidraw` sources, templates, and exported PNGs.
- **Act dividers**: 4 diagram files (`act-1-problem`, `act-2-scaffolding-stack`, `act-3-stress-test`, `act-4-widening`) — each an Excalidraw scene + PNG. Generator: `diagrams/dividers/_gen_dividers.py`.
- **Gap inline diagrams** (new figures that extend each chapter to 4-5 figs):
  - ch01-fig4: "The Failure Surface Stretches"
  - ch05-fig4: "Misassembly Is Not Hallucination"
  - ch05-fig5: "RAG Is Four Different Jobs"
  - ch06-fig4: "Agency Is a Dial, Not a Switch"
  - ch07-fig4: "Enterprise MCP Has One Shape"
  - ch08-fig4: "Half-Duplex Is the Ceiling"
  - ch08-fig5: "Mask Latency, Don't Just Minimize It"
  - ch09-fig4: "Review Is the New Bottleneck"
  - All wired into `public/diagrams/manifest.json` + `src/data/diagram-manifest.json`.
- **llms.txt** — machine-readable book for LLMs and agents (llmstxt.org standard):
  - `website/public/llms.txt` — overview + per-chapter links
  - `website/public/llms-full.txt` — entire book as clean markdown
  - `website/public/read/*.md` — 10 per-chapter markdown files
  - Generator: `website/scripts/gen-llms.mjs` (hooked into prebuild)
  - `robots.txt` updated with LLM file pointers
  - Landing page: "For AI agents" section with links to llms.txt / llms-full.txt / sitemap
- **VisualGuide**: new "The four acts" section renders the 4 act dividers with captions; `manifest.ts` typed.
- **Versions**: `versions.json` + `judge-scores.json` bumped to latest commit hashes; ch02/03/04/08/09 prose-review version snapshot files added.

## OPEN items (do these next)
1. **#29 Debnath (`hwCmfThIiS4`) — known limitation.** VTT artifact in the relevant region means the masking quote can't be cleaned. Claim 29 stands on Zeghidour/Harries/Kramer. Only revisit if you want to swap the source.
2. **Diagrams — right-edge text clipping (needs manual Excalidraw → PNG re-export).** No programmatic export path in this repo.
   - `diagrams/inline/ch10-fig2-constrained-delegation`: five blue/orange boxes clip at the right edge (`what "done" means`, `a workplace`, `the slice`, `trusted authority`). The `.excalidraw` text is correct — just widen the boxes and re-export on excalidraw.com.
   - 5 concept cards same issue (widen canvas + re-export): `14-one-shot-ai-failure`, `15-software-factory`, `16-spec-driven-development`, `17-vibe-coding`, `18-voice-agents`.
3. **MASH re-score** (BLOCKED on you): needs `ANTHROPIC_API_KEY` in shell + ~$11 spend.
   - **Run via Poetry:** `cd ~/Dev/LifeOS/book-mash && poetry run book-mash measure --config ~/Dev/LifeOS/knowledge-bases/ai-engineer-book/book-mash.toml`
   - **Cost preview first:** add `--dry-run` (1,271 calls / $11.38 cold; warm reruns ~90% cheaper).
   - Then: `cd ~/Dev/LifeOS/knowledge-bases/ai-engineer-book && python3 99_Meta/scripts/build_judge_scores.py`
   - book-mash `_CONCURRENCY` lowered 8→3 (uncommitted in book-mash) to avoid rate-limit.

## Method notes
- **Anchor CLI:** `python3 99_Meta/scripts/anchor/cli.py <video_id> "<verbatim phrase>"` → JSON {start,end,quote,confidence}. For ids starting with `-`, use `cli.py -- -<id> "phrase"`.
- **Transcript keyword helper:** `/tmp/kw.py <video_id> "<keyword>" [chars]` (recreate if needed: reads `99_Meta/transcripts/plain/<id>.txt`).
- **Regenerate evidence:** `python3 99_Meta/scripts/anchor/build_evidence.py` (writes `website/src/evidence.json`).
- **drafting ↔ content sync:** edit `public/drafting/Chapter N - ….md` (canonical), propagate body to `website/src/content/chapter-NN.md`.
- **Stats regen:** `python3 99_Meta/scripts/build_stats.py`. CI bot pushes `[skip ci]` stat commits to origin/main — do `git fetch` + reconcile before pushing.
- **Fidelity rule:** never fabricate; every quote/claim/sharpening traces to a source already present.
- **llms.txt regen:** `cd website && node scripts/gen-llms.mjs` (also runs automatically as part of `npm run build` prebuild).

## Local dev
- `cd website && DISABLE_HMR=true npm run dev` → :3002 (may be dead). Routes: `/read/NN-slug`, `/visual-guide`, `/visual-guide/concepts/:slug`.
