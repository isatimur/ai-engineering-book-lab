# Task: sync sources

Goal: refresh the current source inventory, compare to existing corpus, ingest only missing items, and update affected indexes/stats conservatively.

Outputs:
- sync summary
- new items list
- updated indexes/stats
- note of any failures or blocked sources
